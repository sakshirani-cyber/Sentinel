/**
 * useTheme Hook - Convenience Re-export
 * 
 * This file provides a direct import path for the useTheme hook.
 * 
 * @example
 * import { useTheme } from '@/theme/useTheme';
 * 
 * Or simply:
 * import { useTheme } from '@/theme';
 */

export { useTheme, cssVar, cssVars } from './ThemeProvider';
export type { ThemeMode } from './ThemeProvider';
